/**
 * Start the game
 */
function start() {
  initialize();
  requestAnimationFrame(frame);
}

/**
 * Initial actions before
 */
function initialize() {}

/**
 * One frame in the game
 */
function frame(time) {
  requestAnimationFrame(frame);
}
